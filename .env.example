# LogEngine - Configuration Example
# Copier ce fichier vers .env et modifier les valeurs

# PostgreSQL
POSTGRES_DB=logengine
POSTGRES_USER=logengine
POSTGRES_PASSWORD=logengine_password
POSTGRES_PORT=5432

# RabbitMQ
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest
RABBITMQ_PORT=5672
RABBITMQ_MANAGEMENT_PORT=15672

# Database URI (pour connexion locale)
DB_URI=postgres://logengine:logengine_password@localhost:5432/logengine?sslmode=disable

# RabbitMQ URI (pour connexion locale)
RABBITMQ_URI=amqp://guest:guest@localhost:5672/

# Serveurs
HTTP_PORT=8080
GRPC_PORT=30001

# Admin par défaut
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# Environment
ENV=development
